import 'package:flutter/material.dart';

class CreateSupplierPage extends StatelessWidget {
  const CreateSupplierPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.yellow.shade100,);
  }
}